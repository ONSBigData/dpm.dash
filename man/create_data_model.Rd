% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions.R
\name{create_data_model}
\alias{create_data_model}
\title{Create a Custom dpmaccount Data Model}
\usage{
create_data_model(
  dm_name,
  series_name,
  dm_type,
  counts_df,
  time_select = NULL,
  age_select = NULL,
  uncertainty_df = NULL,
  scale_df = NULL,
  disp = NULL,
  scale_ratio = 0,
  ratio = 1,
  sd_scaler = 1,
  sd_overide = -1,
  min_sd = 0,
  count_scaler = 1
)
}
\arguments{
\item{dm_name}{A character string specifying the name of the data model.}

\item{series_name}{A character string indicating the name of the time series within the data model.}

\item{dm_type}{A character string defining the type of data model to create. Valid options are:
\itemize{
\item "Exact Data Model"
\item "Normal Data Model"
\item "T-Dist Data Model"
\item "Negative Binomial Data Model"
\item "Poisson Data Model"
}}

\item{counts_df}{A data frame containing the count data for the time series. It should have columns corresponding to time periods and the observed counts.}

\item{time_select}{(Optional) A subset selection of years to filter the data used in the datamodel to.}

\item{age_select}{(Optional) A numeric subset selection of ages to filter the data used in the datamodel to.}

\item{uncertainty_df}{(Optional) A data frame containing standard deviation information for the "Normal Data Model." Only used if \code{dm_type} is "Normal Data Model."}

\item{scale_df}{(Optional) A data frame containing scale information for the "T-Dist Data Model." Only used if \code{dm_type} is "T-Dist Data Model."}

\item{disp}{(Optional) A numeric value specifying the dispersion parameter for the "Negative Binomial Data Model." Only used if \code{dm_type} is "Negative Binomial Data Model."}

\item{scale_ratio}{(Optional) A numeric value adjusting the scale of the data model (default is 0). Can be used to accommodate differences in units or magnitude.}

\item{ratio}{(Optional) A numeric coverage ratio to apply to the counts (can be a single value or a df)}

\item{sd_scaler}{(Optional) A numeric scale term to apply to the uncertainty (default is 1)}

\item{sd_overide}{(Optional) A numeric overide for the uncertainty, applies to all cells, default -1 to not apply}

\item{min_sd}{(Optional) A lower bound on the uncertainty, anything found lower than this is imputed to the minimum.}

\item{count_scaler}{(Optional) A numeric scale term to apply to the count (default is 1)}
}
\value{
An object of class \code{accountTMB_datamod} representing the created data model. This object can be used in subsequent dpmaccount analyses.
}
\description{
This function creates a data model object (of class \code{accountTMB_datamod}) tailored for use in dpmaccount, a package for accounting time series modeling. The function supports various data model types based on the underlying distribution assumptions.
}
\details{
This function streamlines the creation of data models compatible with dpmaccount. It offers flexibility by supporting different distribution types to match your specific data and analysis needs.
}
\examples{
# Example usage (assuming you have appropriate data frames):
time <- rep(2011, 4)
count <- c(10, 15, 20, 22)
age <- rep(0, 4)
cohort <- c(2010, 2011, 2010, 2011)
sex <- c("Male", "Male", "Female", "Female")

my_counts_df <- data.frame(
  time = time,
  age = age,
  cohort = cohort,
  sex = sex,
  count = count
)
my_datamod <- create_data_model(
  dm_name = "MyModel", series_name = "births",
  dm_type = "Exact Data Model", counts_df = my_counts_df
)

}
