% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions.R
\name{parse_values}
\alias{parse_values}
\title{Parse Numeric Ranges or Text Values from a String}
\usage{
parse_values(input_string)
}
\arguments{
\item{input_string}{A character string containing comma-separated values. Values can be numeric (e.g., "1,2,3"), numeric ranges (e.g., "1:3,5"), or text (e.g., "Male, Female").}
}
\value{
A vector of unique values. If the input contains numeric values or ranges, a numeric vector is returned. If the input contains text values, a character vector is returned.
}
\description{
Parses a comma-separated string containing either numeric values, numeric ranges (e.g., "1:5"), or plain text values, and returns a unique vector of parsed values.
}
\details{
The function first checks whether the input string contains numeric characters or range indicators (":").
\itemize{
\item If numeric, it splits the string by commas, trims whitespace, and expands any ranges (e.g., "1:3" becomes 1, 2, 3).
\item If non-numeric, it returns a unique set of trimmed text values.
Invalid numeric entries or malformed ranges are ignored.
}
}
\examples{
parse_values("1,2,3") # Returns: 1 2 3
parse_values("1:3,5") # Returns: 1 2 3 5
parse_values("Male, Female") # Returns: "Male" "Female"
parse_values("10:8, 2, 4") # Returns: 10 9 8 2 4
parse_values("a, b, c, a") # Returns: "a" "b" "c"

}
